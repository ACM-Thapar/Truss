import { ComponentStyleConfig } from '@chakra-ui/theme';

export const Button: ComponentStyleConfig = {
    baseStyle: {
        borderRadius: '10px',
        color: '#5596E6',
        cursor: 'pointer',
        outline: "none",
        border: "none",
    },
}